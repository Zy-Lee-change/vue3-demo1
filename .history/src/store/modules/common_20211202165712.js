const state = {
  loginStatus: false,
  token: "",
};

const getters = {};

const mutations = {};

const actions = {
  logout() {
    console.log("logout");
  },
};

export default { state, getters, mutations, actions };
